# Copy this file to .streamlit/secrets.toml and fill in your keys.
# Never commit secrets.toml to git.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"

# Optional — required for Claude + GPT-4o cross-validation
OPENAI_API_KEY = "sk-your-openai-key-here"

# Login credentials (username = password) — add/remove users as needed
[demo_users]
admin = "trustlayer2026"
demo  = "demo123"
judge = "hackathon2026"
